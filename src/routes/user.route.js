import {Router} from "express";  // only imports Router function for expressm module

const router= Router();

router.route("/register").
router.route("/login").
router.route("/my_orders").
router.route("/posts").
router.route("/post_comments").
router.route("/post_likes").
router.route("/my_products").


export default router;
